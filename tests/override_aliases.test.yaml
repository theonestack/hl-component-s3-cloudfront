test_metadata:
  type: config
  name: override_aliases
  description: tests setting override aliases

aliases:
  - www.example.com

origins:
  s3bucket:
    source: s3

behaviours:
  default:
    TargetOriginId: s3bucket